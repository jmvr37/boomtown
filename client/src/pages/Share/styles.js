const styles = theme => ({
  sharePage: {
    display: "flex",
    width: "calc(100% + 24px)",
    margin: "-12px",
    justifyContent: "space-between",
    padding: "50px 150px"
  }
});

export default styles;
