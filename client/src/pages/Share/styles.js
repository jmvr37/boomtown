const styles = theme => ({
  sharePage: {
    display: "flex",
    direction: "row",
    justifyContent: "space-evenly",
    alignContent: "center",
    padding: theme.spacing(8)
  }
});

export default styles;
